/**
 * Paste-friendly, first-person narration of the Certification (failed) notes.
 * Backticked spans render as click-to-copy command chips in Script mode;
 * "## " paragraphs render as section headings. Copying strips both.
 */
export const CERT_FAILED_SPOKEN = [
"|| Go to the Guide view for this section. ||"
].join("\n\n");
